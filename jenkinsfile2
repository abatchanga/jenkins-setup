pipeline {
    agent any
    tools{
        maven 'M2-HOME'
    }
    stages{
        stages('maven commands'){
            steps{
                sh 'mvn clean compile install package'
            }
        }

    }
}